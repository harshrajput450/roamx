import { AnnouncementStrip } from '../types';

// Production: No hardcoded mock announcements. Supabase PostgreSQL is the single source of truth.
export const initialAnnouncements: AnnouncementStrip[] = [];
